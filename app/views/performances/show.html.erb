<div class="min-h-screen bg-base-100">
  <section>
    <div class="max-w-5xl mx-auto px-4 pt-6 sm:px-6 space-y-6">
      <%= render "shared/page_header", title: @performance.name, back_path: assembly_performances_path(@assembly), back_label: "Podujatia" %>

      <div class="space-y-6">
        <div class="flex flex-wrap gap-4 text-sm text-base-content/70">
          <span>
            <span class="font-semibold text-base-content">Dátum:</span>
            <%= @performance.date.strftime("%d. %m. %Y") %>
            <% if @performance.end_date.present? && @performance.end_date != @performance.date %>
              – <%= @performance.end_date.strftime("%d. %m. %Y") %>
            <% end %>
          </span>
          <% if @performance.location.present? %>
            <span>
              <span class="font-semibold text-base-content">Miesto:</span>
              <%= @performance.location %>
            </span>
          <% end %>
          <% if @performance.category_names.present? %>
            <span>
              <span class="font-semibold text-base-content">Kategórie:</span>
              <%= @performance.category_names %>
            </span>
          <% end %>
        </div>

        <% if @performance.description.present? %>
          <div class="prose prose-lg max-w-none">
            <%= @performance.description %>
          </div>
        <% end %>
      </div>

    </div>
  </section>

</div>
